# 交接紀錄

最後更新：2026-08-18（台北時間傍晚）

系統本身已經在自動運行，**不需要每天做任何事**。這份文件只記錄一件還沒做完的工作：
歷史資料回補。

---

## 一、系統現況（正常運作，不用處理）

| 項目 | 狀態 |
|---|---|
| 網頁 | https://girlmagician.github.io/ai-daily-digest/ |
| 自動執行 | GitHub Actions，每天 UTC 23:23（台北 07:23） |
| 來源 | 58 個 RSS + 2 個爬蟲（Anthropic News、The Batch） |
| 首頁內容 | 2026-08-18，26 則 |
| 上次 CI 執行 | 成功，663 秒，成本 US$0.7 |

判斷有沒有正常更新：看頁面副標的「產生於 ○○/○○ ○○:○○」。失敗時 GitHub 會寄信。

---

## 二、還沒做完的工作：歷史回補 8/10–8/17

### 已完成（4 天，已產生頁面）

| 日期 | 則數 | 成本 |
|---|---|---|
| 2026-08-10 | 26 | US$1.59 |
| 2026-08-11 | 25 | US$1.01 |
| 2026-08-12 | 26 | US$2.54 |
| 2026-08-13 | 27 | US$0.93 |
| **小計** | **104 則** | **US$6.07** |

### 未完成（4 天）

`2026-08-14`、`2026-08-15`、`2026-08-16`、`2026-08-17`

8/14 在翻譯階段被中止，**沒有留下半套頁面**（每天是原子性的：`render.py` 跑完才算完成，
已發布清單也才會更新）。那次中止浪費了 8/14 的部分費用，重跑會從頭來。

### 待決定：成本超出估計 2.4 倍

當初估整批 US$5，實際四天就花了 US$6.07，八天會是 **US$11～12**。

原因：回補每一天的輸入量都比常規執行大——每天有 30～80 則候選送進策展（常規約 30 則），
而且補抓到的原文更長（中位數 4,000～6,000 字，常規約 2,700 字）。

**剩下四天預估再花 US$5～6。** 這是訂閱額度的消耗，不是額外扣款。

### 要繼續的話

```powershell
cd "D:\vscode ai\ai-daily-digest"
python backfill.py --from 2026-08-14 --to 2026-08-17 --top 30
```

約 40 分鐘。已完成的 8/10–8/13 不會被重跑——那些項目已經記在
`state/seen.json`（目前 176 筆），回補時會自動排除。

想先看每天撈得到多少而不花錢：加 `--dry-run`。

### 要放棄剩下四天的話

不用做任何事。已完成的四頁是完整可用的，「近 7 日」切換與歷史存檔都已經連上。
只是 8/14–8/17 會是空缺。

---

## 三、還沒推送 / 還沒生效的東西

### 1. 有 1 個 commit 還沒推送

```powershell
git pull --rebase
git push
```

`644ea76 加入歷史回補` — 加上這次回補產生的 4 個頁面（見下）。

### 2. 新版面還沒上線

以下改動已經提交，但**線上網頁還是舊版**，因為需要跑一次 CI 才會重新產生：

- 寬度 1280px
- 兩欄卡片排列
- 分類篩選器（捲動時固定在頂端）
- 近 7 日日期切換
- 新增 3 個商業來源：The Information、Bloomberg Technology、FT 人工智慧

推送後手動觸發一次即可生效（也可以直接等隔天早上 07:23 自動執行）：

→ https://github.com/girlmagician/ai-daily-digest/actions → 每日 AI 情報 → Run workflow

### 3. 回補產生的頁面還沒提交

`docs/2026-08-10.html` ～ `docs/2026-08-13.html`、`docs/archive.html`、`state/seen.json`

注意：這 4 頁是**舊版面**產生的（回補在版面改動之前就開始跑）。如果希望它們也套用
兩欄卡片，推送後執行：

```powershell
python render.py --replay     # 只會重畫首頁那天
```

`--replay` 只重畫最後一次發布的內容，不會重畫回補的那 4 天。要讓回補頁面也換版面，
得重跑 `backfill.py`（會再花一次錢），或接受它們維持舊版面——內容完全正確，只是排版不同。

---

## 四、幾個已知限制（無法解決，只能揭露）

- **論文回補不到。** arXiv 的 RSS 只有當日更新，8/10–8/17 的論文全部拿不到。
  要回補得改用 arXiv API。
- **越早的日期來源越薄。** RSS 只保留最近幾則，8/16 只有 9 家來源、33 則候選，
  取 30 則等於幾乎不篩選，品質會低於其他天。
- **The Batch 每天都會失敗。** GitHub 的資料中心 IP 被 deeplearning.ai 回 403
  （本機正常），會固定出現在頁尾的「本次未取得來源」。這是誠實揭露，不是漏失。
- **FT 人工智慧的摘要會很短。** 文章在付費牆後抓不到全文，只能用 74 字的 feed 導言。
  它的價值在「有沒有報導這件事」的訊號，權重已壓到 1.1。
- **Reuters 拿不到。** 公開 RSS 已關閉（401/404）。要補通訊社等級的覆蓋只能走
  GDELT DOC API，尚未實作。

其他技術決策與踩過的坑寫在 `README.md`、`sources.yaml` 的 `dropped` 與
`known_issues`，以及各模組的註解裡。

---

## 五、本機操作備忘

- **CLI 要用 `claude.cmd`**，不是 `claude`。PowerShell 的執行原則會擋 `.ps1`，
  不要去改 `Set-ExecutionPolicy`（公司群組原則通常也會擋）。
- **推送前一定先 `git pull --rebase`。** 工作流程每天早上會自己 commit 產出，
  遠端幾乎永遠領先本機，直接 push 會被拒絕。
- **只想調版面**：`python render.py --replay`，不呼叫模型、不花錢、三秒完成。
