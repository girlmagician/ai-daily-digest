# 交接紀錄

最後更新：2026-08-19（台北時間下午）

系統已經在自動運行，**不需要每天做任何事**。歷史回補（8/10–8/17）已於今天全部完成，
目前沒有待辦工作。

---

## 一、系統現況

| 項目 | 狀態 |
|---|---|
| 網頁 | https://girlmagician.github.io/ai-daily-digest/ |
| 自動執行 | GitHub Actions，每天 UTC 23:23（台北 07:23） |
| 沒跑到的補救 | 看門狗每天早上 09:17 / 11:17 / 13:17 檢查，沒產出就自動重跑，重跑仍失敗才開 issue |
| 來源 | 58 個 RSS + 2 個爬蟲（Anthropic News、The Batch） |
| 首頁內容 | 2026-08-19，22 則 |
| 歷史存檔 | 8/10–8/19 連續十天 |
| 已發布清單 | `state/seen.json` 337 筆（保留 21 天） |
| 版面 | 1280px、兩欄卡片、分類篩選器、近 7 日切換 — 已上線 |
| 選稿 | `collect --top 45` → `translate --target 30`；分數下限 1.6 |
| 本機 git | 與 origin/main 同步，工作區乾淨 |

判斷有沒有正常更新：看頁面副標的「產生於 ○○/○○ ○○:○○」。

**不要只靠通知信。** 執行失敗時 GitHub 會寄信，但**排程被跳過時不會**——沒有失敗的執行，
就沒有信。2026-08-26 那次就是這樣：連續八天正常之後，UTC 23:23 那一刀根本沒有被建立出來，
信箱一片安靜，網站默默停在前一天。GitHub 的 `schedule` 本來就是 best-effort，不保證會跑。

現在由 `.github/workflows/watchdog.yml` 補這個洞：每天早上三個時段檢查
`docs/<台北日期>.html` 在不在，沒有就自動重跑，重跑仍然生不出東西才開 issue 通知你。
所以**信箱安靜等於一切正常**這件事，從 2026-08-27 起才成立。細節見
[`SETTINGS.md`](SETTINGS.md) 第一節。

---

## 二、歷史回補：已完成

| 日期 | 則數 | 成本 |
|---|---|---|
| 2026-08-10 | 26 | US$1.59 |
| 2026-08-11 | 25 | US$1.01 |
| 2026-08-12 | 26 | US$2.54 |
| 2026-08-13 | 27 | US$0.93 |
| 2026-08-14 | 25 | US$1.29 |
| 2026-08-15 | 29 | US$1.05 |
| 2026-08-16 | 20 | US$1.45 |
| 2026-08-17 | 27 | US$1.01 |
| **合計** | **205 則** | **US$10.87** |

當初估整批 US$5，實際 US$10.87（2.2 倍）。原因：回補每一天的輸入量都比常規執行大——
每天 23～43 則候選送進策展（常規約 30 則），補抓到的原文也更長。
**日後估這類長時間、耗訂閱額度的作業要抓兩倍以上，並且中途就回報，不要跑完才說。**

不用再跑 `backfill.py` 了。真要重跑某一天，記得 `state/seen.json` 會排除已發布過的項目，
需要 `--ignore-seen` 才會重新納入。

---

## 三、已知限制（無法解決，只能揭露）

- **收藏不跨裝置、也不跨瀏覽器。** 存在 localStorage，換裝置或清除網站資料就會消失。
  唯一的退路是收藏頁的「匯出 JSON」，累積到一定數量建議匯出備份一次。
  要同步就得有帳號與資料庫，那會讓這個專案從靜態站變成一套服務，目前判斷不划算。
- **論文回補不到。** arXiv 的 RSS 只有當日更新，8/10–8/17 的論文全部拿不到。
  要回補得改用 arXiv API。
- **越早的日期來源越薄。** 8/16 只撈到 23 則候選（`--top 30` 等於不篩選，策展仍剔掉 3 則），
  只有 20 則上稿，是十天裡最少的。品質低於其他天是來源使然，不是程式問題。
- **The Batch 每天都會失敗。** GitHub 的資料中心 IP 被 deeplearning.ai 回 403
  （本機正常），會固定出現在頁尾的「本次未取得來源」。這是誠實揭露，不是漏失。
- **FT 人工智慧的摘要會很短。** 文章在付費牆後抓不到全文，只能用 74 字的 feed 導言。
  它的價值在「有沒有報導這件事」的訊號，權重壓到 1.1。加上分數下限 1.6 之後，
  它要有 HN 分數或跨來源佐證才上得了稿——這是刻意的。
- **arXiv 每週日、週一必定出現在「未取得的來源」，那是正常的。**
  arXiv 只在美東時間週日到週四晚上 20:00 announce，週五、週六晚上沒有。
  feed 每天 04:00 UTC 更新，而我們的排程在 23:23 UTC——換算下來，
  **台北時間週日與週一那兩份日報一定拿到空的 arXiv feed**，
  頁尾會列出 `arXiv cs.AI／cs.CL／cs.LG — 解析不到項目` 三行。

  2026-08-23（日）與 08-24（一）實際發生過，其餘八天都正常。
  這是 arXiv 的發佈節奏，不是程式壞掉，也不需要修。
  唯一的副作用是那兩天的「研究與論文」分類會比平日空。
  看到這三行不用查，看到**週二到週六**出現才要查。

- **廠商 feed 可能「還活著但內容搬走了」——這類失敗沒有任何自動偵測。**
  2026-08-27 抓到實例：Google 把模型發布搬到 `/innovation-and-ai/models-and-research/`，
  我們訂的 `/technology/ai/` 仍回 200、仍每天更新，只是內容剩下消費性行銷貼文
  （「用 Google Search 升級家飾」）。結果是 Gemini 3.5 Transcribe 完全沒進日報，
  而 8/13 的 Gemini 3.7 Flash 只是碰巧被 HN 967 分撈回來——同一類公告，
  Transcribe 只有 HN 15 分（門檻 20），差五分就消失。

  更糟的是那三個 Google 來源都設 `pinned: true`（官方公告當日一律收錄、豁免分數下限），
  但 pinned 只在項目**進入候選池之後**才生效。沒進池子，安全網等於被拆掉而且沒有訊號。

  已修（見 `sources.yaml` 的註解與 `known_issues`）：新增 `Google Models & Research`
  與 `Google Developers Blog`、把 `Google AI Blog` 從 2.0/pinned 降到 1.2/不 pinned。
  實測改完之後 Transcribe 以 2.383 分進保障名額，家飾貼文以 1.323 分被自動剔除。

  **這個教訓對每一個廠商 feed 都成立**，不是 Google 專屬。新鮮度檢查抓不到它
  （那個 feed 顯示「1 天前更新」）。唯一可行的是定期人工抽查：拿廠商官網最近的重大發布，
  回頭確認日報有沒有收到。Microsoft Source AI 已查過，是單純發文頻率低，不是搬家。

- **開源釋出層是刻意的盲區，不是漏抓。** 這條管線覆蓋的是「關於 AI 的新聞」——媒體報導、廠商公告、
  論文、融資。它幾乎不覆蓋「今天 GitHub 上放出了什麼」。`sources.yaml` 裡跟 GitHub 有關的只有
  llama.cpp／vLLM／Ollama 三個 `releases.atom`，那是追蹤既有專案的新版本，不是發現新專案。
  唯一的間接路徑是 HN，而 `hn.py` 的 `MIN_POINTS = 20` 對新 repo 是很高的門檻。

  2026-08-27 拿另一個排程撈到的七個 repo 實測：只有兩個上過 HN（cetus 2 分、headlong 3 分），
  其餘五個 HN 上完全找不到，七個在當天日報全文零命中。其中 `shuohao-skills` 2,176★、
  `grok-bot-0.18-reconstructed` 3,086★——星數比日報裡多數項目的熱度都高，但這裡看不見。

  **盲區明顯偏向中文生態**：量子位、遊戲陀螺、iThome 覆蓋的是中文*新聞*，中文圈的 repo 釋出
  （阿里 TLive-Omni、螞蟻 4DAnyone、shuohao-skills）沒有任何管道，因為 HN 是英語社群。

  補這個洞的兩條路都評估過並否決了，理由與實測數字寫在 `sources.yaml` 的 `dropped`
  （The Rundown AI、GitHub 星數排行）。結論是**維持現狀**：另一個排程已經在做 repo 發現，
  而且做得比這條管線可能做到的更好。兩個工具互補，不是重複。你會漏的是「業界在動什麼手」，
  不是「業界發生什麼事」——知道盲區的形狀就好，不要硬合併。

- **看門狗的三個時段有可能全被跳過。** 跳過一次很常見（8/26 就發生了），三次全中機率很低，
  但不是零。真要保證得靠 GitHub 以外的東西定時來戳，代價是要把 token 放到外部服務，
  目前判斷不划算。萬一真的連續兩天沒更新而你也沒收到 issue，就是撞到這個。
- **Reuters 拿不到。** 公開 RSS 已關閉（401/404）。要補通訊社等級的覆蓋只能走
  GDELT DOC API，尚未實作。

其他技術決策與踩過的坑寫在 `README.md`、`sources.yaml` 的 `dropped` 與
`known_issues`，以及各模組的註解裡。

---

## 四、選稿規則（2026-08-19 調整）

**完整的參數值與「想改什麼改哪裡」請看 [`SETTINGS.md`](SETTINGS.md)。**
這裡只記錄這次為什麼要調、以及接下來要觀察什麼。

起因是抽查了幾則「為什麼這也會上稿」：一則 twitter 個人推文（HN 151 分）、
一則 FT 的 74 字導言、一則 llama.cpp 的 CUDA 參數 commit。追下去發現根本原因是
**候選數與目標數都是 30**——只要擠進機械評分前 30 名，幾乎就等於會上稿，
策展編輯沒有「必須裁掉誰」的壓力。四項調整：候選池加深到 45、社群貼文熱度乘 0.5
並標示、一般新聞加分數下限 1.6（官方公告與論文豁免）、HN 項目改標「投稿 HN」。

成本影響：只有策展那一次呼叫的輸入變大，實測增幅 5% 以內。真正的代價是
`extract.py` 多抓 15 篇原文，CI 多跑 1–2 分鐘。

**接下來要觀察的**：

- **則數：已觀察完畢，結論是不要動下限（2026-08-27 結案）。**
  原本的指示是「連續幾天掉到 20 則以下就把下限 1.6 調成 1.5」。8/20–8/27 實測：

  | 8/20 | 8/21 | 8/22 | 8/23 | 8/24 | 8/25 | 8/26 | 8/27 |
  |---|---|---|---|---|---|---|---|
  | 30 | 30 | 27 | 30 | 29 | 30 | 30 | 29 |

  八天全在 27–30，其中五天剛好卡在 30——那是 `--target 30` 的上限。
  **警報從來沒有響過，下限 1.6 不要動。**

  而且瓶頸已經換邊了：候選池加深到 45 之後供給變充足，現在決定則數的是**目標值 30**，
  不是分數下限。下限只在來源稀薄的日子才會起作用。這跟當初預期的方向相反，
  所以那條舊指示已經失效——**不要再照它去調 1.5**，那只會在供給充足時放進更多低分項目。

  真要調則數，改的是 `translate --target`，不是 `MIN_SCORE`。
- **降權不等於排除。** 那則印表機推文從 2.145 降到 1.838，仍在下限之上，
  還是會上稿——只是排名下降並標了「社群貼文」。如果你要的是完全不出現，
  得改成排除社群網址，但那會連各家 AI 公司發在 X 上的重大宣布一起殺掉。
- **舊存檔頁不會回溯。** 「社群貼文」與「投稿 HN」只影響之後產生的頁面
  （8/19 那頁因為 `--replay` 重畫過，已經套用）。

## 五、AI_KEYWORDS 補模型家族名（2026-08-27）

起因是追查 Gemini 3.5 Transcribe 漏收時，發現 `ai_filter: true` 的來源會誤殺
`Gemma 4 QAT models`、`Inside the Gemmaverse`、`AlphaEvolve`——**「Gemma」不在關鍵字表裡**。
往下追發現真正的大洞不在 feed，而在 HN 補件：`merge_hn` 只有標題可判，
只寫模型名而不寫「AI」的標題全被擋掉。

實測 HN 近 14 天有 **11 則**被擋，而且**這 11 則的網址在任何一天的日報裡都找不到**：

| HN 分數 | 標題 |
|---:|---|
| 802 | Qwen 3.8 27B is excellent, but it defaults to overthinking things |
| 648 | Qwen3.8-Flash-Next |
| 498 | DeepSeek-v4-flash-vision-exp |
| 381 | Qwen3.8 27B scores 52 on Artificial Analysis |

`qwen.ai` 這個網域從來沒有出現在任何一天的日報裡。

補了 19 個詞（`qwen`／`deepseek`／`gemma`／`grok`／`nemotron`／`mixtral`／`codestral`／
`midjourney`／`hunyuan`／`doubao`／`zhipu`／`modelscope` 與對應中文）。

**一個容易誤判的陷阱**：主題有進日報 ≠ 那則高分貼文有進。`merge_hn` 是先用網址把
HN 分數併到既有項目（這條路**沒有**關鍵字關卡），只有「新增」HN 項目才過 `ai_relevance`。
所以日報裡看得到 Qwen，不代表 Qwen 的官方發布有被收錄。

**`AI_PATTERN` 是無邊界的子字串比對**，加詞前一定要用陷阱字串測過。這次排除掉的與理由
（`phi` 命中 graphics、`yi` 命中 playing、`wan` 命中 Taiwan、`kimi` 命中日文的「君」…）
逐條寫在 `collect.py` 的註解裡，不要重蹈。

**接下來要觀察的**：候選池會略為變大。實測 feed 端只多 0.8%、HN 端約每天多一則，
成本影響可忽略，但如果哪天發現一堆不相干的東西湧進來，第一個要懷疑的是這裡新加的詞。

## 六、繁簡混雜的修正（2026-08-31）

使用者回報摘要不是全繁體。查證結果：**22 天 553 則裡有 6 則整段簡體**，
其中 5 則集中在 8/29。

決定性的證據是把 `state/last-digest.json` 的項目依 `BATCH_SIZE = 5` 切開來看：

| 批次 | 索引 | 狀況 |
|---|---|---|
| 第 1 批 | 0–4 | 乾淨 |
| **第 2 批** | **5–9** | **五則全部簡體** |
| 第 3–5 批 | 10–22 | 乾淨 |

**一次 LLM 呼叫整批回簡體，同一天其他四次完全正常。**

兩件事排除了直覺的猜測：

- **不是簡中來源直接放行。** 那五則裡四則來自英文來源（Hacker News、Simon Willison）。
- **不是提示詞沒講清楚。** 那批的用語是台灣的——「代理程式」不是「程序」、
  「軟體」不是「软件」。模型照指示翻了，只是字形輸出成簡體。

真正的缺口在程式端：`verify()` 檢查 id、網址、腦補、長度，**就是沒有檢查字形**。

**修法**：`verify()` 加繁簡檢查，失敗會進既有的重試迴圈（`attempts=3`，錯誤訊息會
回饋給模型）。同時在提示詞補一條絕對規則做預防。

**為什麼門檻訂得很寬（8 個字或佔比 15%）**：批次連續三次驗證失敗會 `sys.exit`
中止整份日報。553 則的實測分布是——真正出問題的 6 則有 65～87 個簡體字，
次高的只有 4 個（多為專有名詞，「騰訊」「藍馳」寫成簡體），中間是巨大空隙。
門檻落在空隙正中央，回測 6/553 全中、零誤判。

**代價要說清楚**：1～4 個字的零星簡體（如「游戲」該作「遊戲」）**不會被抓**。
那是刻意的取捨——為了幾個字讓整天的日報消失不成比例。

**繁簡不等於內容不可信。** 所以它跟腦補、假網址不同，走軟失敗路徑：重試三次仍
沒改過來時，只要「剩下的錯誤全是繁簡」就放行，並記進 `digest["degraded_batches"]`
與收尾輸出（`⚠ 有 N 批降級放行`）。混著任何內容類錯誤時，一律照舊中止。
**降級一定要留下紀錄**——靜默放行正是這個問題拖了十天才被發現的原因。

## 七、不漏接的四項修正（2026-08-31）

以「訊息正確性與不漏接」為最高原則做的一輪盤點，四項都改完了。

### 0. 追加：`hfpapers.py` — 論文真正需要的訊號

第 1 項改完之後實測發現規則是空的：**48 小時內 HN 上的 arXiv 投稿只有 1 則，
而且對不上當天 feed 的任何一篇**。HN 討論論文是發表後好幾天甚至數週的事，
跟 48 小時的窗幾乎沒有交集。所以「必須有 HN 提及」等於論文區永遠是 0。

補上 `huggingface.co/api/daily_papers`（官方 API、免認證）：**當天**的人工策展
清單且帶投票數，正好是缺的那個訊號。比照 `merge_hn` 的模式接進 `collect.py`：

- 幫既有 arXiv 項目補投票數（實測對上 6 篇，拿到正確的 arXiv 出處與摘要）
- 把不在我們分類 feed 裡的論文補進候選池（實測 16 篇——我們只訂
  cs.AI／cs.CL／cs.LG，HF 會收到 cs.CV、cs.RO 等分類的重要論文）

時間用**上榜時間**而不是 arXiv 發表時間。HF 常把幾天前的論文送上當日榜
（實測榜首發表於 8/28、8/31 才上榜），用發表時間會被 48 小時的窗濾光——
實測直接歸零。`merge_hn` 對 HN 項目也是同樣慣例：用投稿時間。

實測結果：`論文池 518 篇 → 有外部訊號 21 篇 → 選入 5 篇`，全部附 1100–1800 字摘要。

**2026-09-01 首次上線時漏了一步**：`curate()` 組訊號字串時只放 HN 分數、跨來源數與
官方公告，**沒有放 HF 投票**。結果選進來的五篇論文在策展階段全被剔除，理由清一色是
「arXiv 預印本，無社群或媒體關注訊號」——它們正是因為有 HF 投票才被選進來的，
只是策展編輯看不到。已補上 `HF Daily Papers N票`。
**教訓：新增訊號時要一路檢查到底，選進候選池不等於上得了稿。**

`run_report.json` 新增 `papers_pool_size`／`papers_with_signal`／`papers_selected`，
會存進版控的 `state/last-report.json`。**「池子很大但有訊號的是 0」正是這次
之前的長期狀態**，這幾個數字讓它下次一眼看得出來。

### 1. arXiv 論文從來沒有上過稿（最嚴重）

`select()` 把論文寫進 `candidates.json["papers"]`，但 `translate.py` 只讀
`official + ranked`——**那個鍵從來沒有任何地方讀取過**。回頭查 13 天 356 則，
來源是 arXiv 的有 **0 則**。每天抓數百篇，全部丟在地上。

而且就算接上，原本也挑不出重點。截斷的排序是 `published_utc` 由新到舊，
但實測當天 302 篇 cs.AI 的**時間戳完全相同**（都是 04:00:00，同一次 announce），
排序等於沒作用，實際效果是取 feed 的前 8 筆——隨機。

更糟的是順序：截斷發生在 `merge_hn` 之前，**訊號還沒套用就先砍掉九成**。
一篇 300 分的論文若排在 feed 第 150 筆，在 HN 分數有機會貼上去之前就不存在了。

改法三段：
- `PAPER_SOURCE_CAP_RAW = 600`，論文不在收集階段截斷，留給 `select()` 排名
- `select()` 實作 docstring 從一開始就寫著、卻沒做的規則：**論文必須有外部提及
  才收錄**（HN 分數 > 0 或 HF 投票 > 0，見上面第 0 項）。沒有外部訊號的論文無法
  排名，寧可當天沒有論文，也不要放五篇隨機的
- `translate.py` 的 pool 加上 `data["papers"]`

實測全部 668 篇進 `cluster()` 要 21 秒（CI 逾時 75 分鐘），而且會聚成 502 群——
**166 篇是跨分類重複**，那正是原本拿不到的訊號。

### 2. 截斷排序改看相關性

同一個毛病影響綜合型來源：Nikkei xTECH 一天窗內 58 則只留 12 則、4Gamer 砍掉 99 則，
全按新舊而不是相關性，而 `ai_strength` 明明算好了卻沒用。
現在 `ai_filter` 的來源改成先排相關強度、再排新舊（兩段式穩定排序——
寫成單一 tuple 鍵會錯，`published_utc` 是字串無法在遞增排序裡表達「由新到舊」）。

### 3. `hn.py` 的 `MIN_POINTS` 由 20 降到 10

近 14 天落在 10–19 分且通過 AI 相關性判定的有 151 則（每天約 10.8 則）被擋掉，
其中就有使用者回報漏收的 **Gemini 3.5 Transcribe（18 分）**——那次除了 Google feed
搬家之外，這是第二道獨立的閘門，兩道都擋住了它。

實測單次多接住 13 則。成本幾乎不變：候選池有 `--top 45` 上限，送進模型的量不會
跟著長；`extract.py` 多抓幾篇是純 HTTP。雜訊由 1.6 下限與策展擋。
**如果哪天發現論壇閒聊變多，第一個要回頭看的就是這個數字。**

### 4. 新增 `audit_coverage.py`：抓「未知的未知」

拿站台總表 feed 跟我們訂的分類 feed 做差集。這是唯一抓得到「feed 還活著、
但內容搬到別的分類」的機制——日報的「未取得的來源」、`verify_feeds.py`、
新鮮度檢查全都抓不到它。

已接進 `daily.yml`，**`continue-on-error: true`，結果寫進 `out/coverage_audit.txt`
隨 artifact 上傳**。它是諮詢性訊號，永遠不該擋下當天的日報。

首次執行的結果是好消息：blog.google 最近 21 天漏掉的 7 則**全是產品／行銷層級**
（Workspace 開學指南、Khan Academy 合作），**沒有一則是模型發布**——代表 8/27 補的
那兩個來源確實把重要那一層補上了。因此**刻意不加** `/innovation-and-ai/products/`，
它的獨有內容就是那些行銷貼文。

**這支工具做不到的事**：只有提供總表 feed 的站台適用。沒有總表的站台（多數個人
部落格與新聞網站）幫不上忙。它縮小盲區，不消除盲區。

### 順手修的：arXiv 端點

實測 `export.arxiv.org` 的 cs.AI 連續三次回不出項目，`rss.arxiv.org` 三個分類都是 3/3。
改成優先用後者、前者留作備援。**這跟週末空 feed 是兩回事**——週末是 arXiv 沒有
announce，兩個端點都會空。

### 沒有做的：`--target 30`

8/20–8/28 有六天卡在 30，排名 31 名以後的直接消失。但每天要讀幾則是**偏好問題
不是缺陷**，而且直接增加翻譯成本。留給使用者決定。

## 八、觀察中：排程遲到（2026-09-01 起）

**還沒有動任何設定，先觀察幾天。** 這裡記下基準線，免得過幾天忘了當初的數字。

排程是 UTC 23:23（台北 07:23 開跑，正常約 07:50 發布完）。實際發布時間：

| 日期 | 發布（台北） | 相對正常 |
|---|---|---|
| 8/24 | 07:56 | 準時 |
| 8/25 | 07:47 | 準時 |
| 8/26 | 07:48 | 準時 |
| 8/30 | 09:22 | **+1.5 小時** |
| 8/31 | 09:30 | **+1.7 小時** |
| 9/1 | 09:57 | **+2.1 小時** |
| 9/2 | 09:22 | **+1.5 小時** |

8/27–8/29 那幾天有人工補跑與看門狗重跑混在裡面，不是乾淨的排程樣本，故不列入。

**8/26 之前完全準時，8/30 起開始遲到。** 這是 GitHub 排程佇列的延遲，不是我們的
程式變慢——9/1 那次整條管線只跑了 7 分 11 秒。

原本看起來像「逐日增加」，但 9/2 回到 +1.5 小時，**目前是在 +1.5～+2.1 之間浮動**，
不是單調惡化。四天的樣本還不夠判斷，繼續觀察。

**要觀察什麼**：延遲量會不會穩定。

- **穩定在 +2 小時左右** → 可以把 cron 提前兩小時（`23 23` → `23 21`），
  賭延遲後落回台北 07:30 前後。
- **忽大忽小**（像 8/27–8/29 那樣從 +3 到 +7 小時都有）→ **不要動 cron**。
  提前只是猜測性補償，延遲量本身不穩定的話會變成有時太早、有時還是太晚，
  而且提前開跑會讓收集的時間窗相對前一天縮短。

判斷資料就在 `git log` 的「日報 YYYY-MM-DD」commit 時間，不必另外記錄。

**另一件要一起看的：成本。** 8/31 那批改動（HN 門檻 20→10、論文接進管線）上線後，
30 則的日子成本從 0.55～0.71 上升到 9/1 的 0.61、9/2 的 0.82。9/2 是目前最高，
約比先前的 30 則基準高三到四成（月估從 US$18 升到 US$25 上下）。
多出來的是候選池變大與論文的長摘要。單日樣本不足以下結論，但如果連續幾天都在
0.8 以上，`--top`、`hn.MIN_POINTS`、`--max-papers` 是三個可調的旋鈕。

**注意**：看門狗不會因為遲到而誤報。9/1 那天它 09:17 檢查時執行正在進行中，
正確地安靜等待了——這是設計預期內的行為，不是漏掉。

## 九、本機操作備忘

- **CLI 要用 `claude.cmd`**，不是 `claude`。PowerShell 的執行原則會擋 `.ps1`，
  不要去改 `Set-ExecutionPolicy`（公司群組原則通常也會擋）。
- **推送前一定先 `git pull --rebase`。** 工作流程每天早上會自己 commit 產出，
  遠端幾乎永遠領先本機，直接 push 會被拒絕。
- **rebase 撞到 `state/seen.json` 要做聯集，不能取任一邊。** 8/19 那次遠端 113 筆、
  本機 176 筆，交集只有 61 筆。格式是 `{"ids": {項目雜湊: 日期}}`；同一項目兩邊日期
  不同時取**較晚**的，否則會被 `SEEN_DAYS=21` 的剪裁提早丟掉，隔天又重複刊登。
- **rebase 撞到 `docs/archive.html` 直接取遠端版就好**，之後跑一次
  `python render.py --replay` 會依 `docs/*.html` 重算日期清單並重畫。
- **只想調版面**：`python render.py --replay`，不呼叫模型、不花錢、三秒完成。
  它會重畫首頁、`feed.xml`、`archive.html` 與最後發布那天的存檔頁。舊日子的版面不會變
  （那些 digest 沒有保存，要重畫得重跑模型），但導覽會（見下）。
- **`refresh_shell()` 會在每次 render 最後重刷所有存檔頁的外殼（導覽＋收藏腳本）。**
  每頁的日期列是產生當下寫死的，隔天多一天就過期——8/10 那頁一度只連得到 8/18 和 8/10。
  現在改成每次 render 都對既有 HTML 做字串替換把日期列換掉，不重畫內容也不花錢。
  收藏腳本走同一條路注入，所以 8/10 以來的每一頁都能收藏。
  副作用：日期落在最近七天之外的頁面（目前 8/10–8/12），日期列上不會有代表「你在這頁」
  的高亮，因為那一天已經不在窗內。頁首本來就寫著日期，判斷上不會混淆。
